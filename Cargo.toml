[package]
name = "visual-bitcoin-engine"
version = "0.1.0"
edition = "2024"
description = "An educational project to visualize how Bitcoin works"

[dependencies]
# SHA-256 해시를 만들어주는 라이브러리
# 비트코인도 SHA-256을 사용한다.
sha2 = "0.10"
